<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.katiui">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KatiUI">
        <activity android:name=".katiDomain.main.SignOutActivity"></activity>
        <activity
            android:name=".katiDomain.main.PasswordEditActivity"
            android:label="@string/title_activity_password_edit"
            android:theme="@style/Theme.KatiUI.NoActionBar" />
        <activity
            android:name=".katiDomain.main.SignUpActivity"
            android:label="@string/title_activity_sign_up"
            android:theme="@style/Theme.KatiUI.NoActionBar" />
        <activity android:name=".katiDomain.main.view.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>